// Hostinger (Deploy Your Web App) espera por defecto un archivo app.js en la raíz
// del proyecto para detectar automáticamente que esto es una app Node.js/Express.
// La lógica real vive en src/server.js — este archivo solo la arranca.
require('./src/server.js');
